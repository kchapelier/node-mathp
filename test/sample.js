"use strict";

var should = require('chai').should();

var DataFilter = require('../index.js');

describe('Sample', function(){
	describe('sample of sample', function() {
		it('is a test', function() {
			true.should.be.true;
		});
	});
});